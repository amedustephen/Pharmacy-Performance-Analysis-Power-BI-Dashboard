# Deploy

   Dockerfiles, API code (e.g., FastAPI), and cloud deployment configuration metrics.

   .github/workflows/ — Automated pipelines using the Power BI REST API or Fabric CLI to deploy reports to target workspaces.
